tasks:
  literal-eval:
    steps:
      - set:
          - HOST: prod.example.com
          - PORTS: [8080, 9090]
          - CONFIG:
              host: .HOST
              endpoint: "{{.HOST}}/v1"
              replicas: 3
              enabled: true
      - set:
          - ALIASED:
              a: &shared hello
              b: *shared
