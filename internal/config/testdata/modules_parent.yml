modules:
  - _farm: modules_farm.yml
  - yard: modules_yard.yml
    show: [clean, fix]
    hide: [die]
    flatten: true

tasks:
  deploy:
    info: Deploy the app
    steps:
      - call: _farm:milk_cow
