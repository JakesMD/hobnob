tasks:
  get_optional:
    steps:
      - get:
          - NOTE:
              info: "Optional note"
              optional: true
          - SIZE:
              info: "Enter size"
              check: "[ {{.SIZE}} -le 100 ]"
              optional: true
