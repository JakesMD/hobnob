tasks:
  configure:
    steps:
      - set:
          - API_KEY:
              value: "abc123"
              secret: true
          - HOST: "localhost"
          - DB_PASS:
              value: "{{.DB_PASS | default \"changeme\"}}"
              secret: true
