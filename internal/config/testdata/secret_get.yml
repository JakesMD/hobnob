tasks:
  login:
    steps:
      - get:
          - PASSWORD:
              secret: true
              info: "Enter password"
          - USERNAME:
              info: "Enter username"
          - TOKEN:
              secret: true
              default: "default-token"
